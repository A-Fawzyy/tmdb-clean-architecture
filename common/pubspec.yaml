name: common
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.19.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  lint: ^2.0.1

flutter:

flutter_intl:
  enabled: true
  class_name: AppLocalization
  main_locale: en
